{
  "steps": {
    "install": {
      "commands": [
        "npm install"
      ]
    },
    "build": {
      "commands": [
        "npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
